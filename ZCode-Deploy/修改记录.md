# ZCode 人格部署 — 修改记录

> 更新日期: 2026-09-19
> 来源: 本仓库是 [zzooymc-source/ZCode-Deploy](https://github.com/zzooymc-source/ZCode-Deploy) 的修改版
> 当前状态: 已部署（适配 ZCode 2026-09-19 重新打包的 zcode.cjs）
> 目标: 将 ZCode 的 SYSTEM 层完全替换为自定义人格文件

---

## 0. 本次修复：ZCode 更新后补丁全部失效

### 症状

ZCode 更新后，重新跑 `部署.bat` 提示"部署完成"，但开新对话完全没有效果 ——
system 层还是原版 "You are ZCode"。

### 根因

ZCode 在 2026-09-19 20:57 更新，`zcode.cjs` 被换成**重新打包的版本**：

| | 旧版 | 新版 |
|---|---|---|
| 大小 | 11,416,833 bytes | 14,796,490 bytes |
| bundle | — | `source: apps/zcode-cli/packages/cli/dist/zcode.cjs` |

重新打包后**压缩变量名全部变了**，而 v1 脚本的 6 个补丁锚点全是写死的变量名：

- `Djo`（CLI Prefix）→ 新版叫 `FVs`
- `Eut()`（Agent Identity）→ 新版叫 `Qfn()`
- `fre()`（日期提醒）→ 新版叫 `nNe()`
- `sre()`（Skills）→ 新版叫 `KMe()`
- `F1e()`（User Context）→ 新版叫 `Yut()`
- `build()` 内部局部变量也从 `n,o` 变成 `o,s`

实测 6 个锚点**全部 MISSING**（新旧锚点都搜不到），于是每个补丁都走到
`[警告] 未找到目标` 分支，然后照样把**未修改的内容写回去**，最后打印"部署完成"。

**这是 v1 最致命的设计缺陷：补丁没命中时只是黄色警告，仍然继续写文件并报成功。**
用户看到的是"部署完成"，实际什么都没发生。

### 修复（v2）

1. **锚点改为语义字面量**，不再依赖压缩变量名
2. 用**花括号配平**回溯出真正包住锚点的函数，再对该函数动手
3. **任一补丁未命中 → 不写入、退出码 1、打印注入点诊断**
4. 写入前用 `node --check` 校验补丁结果，不通过绝不落盘
5. 备份策略修复：ZCode 更新后 `.bak` 自动刷新为新版原版（见下）

### 顺带修掉的三个隐藏缺陷

| 缺陷 | 后果 | 修复 |
|------|------|------|
| `Get-PSDrive -PSType FileSystem` 参数名错误 | 盘符扫描那一步永远抛错，等于没有 | 改为 `-PSProvider FileSystem` |
| `.bak` 永远是首次部署时的版本 | ZCode 更新后跑 `恢复.bat` 会把应用**降级回旧版** | 检测到当前文件是原版就刷新 `.bak` |
| 安装路径表没有 `D:\app\zcode` | 本机实际装在 `D:\app\zcode`，ZCode 没运行时检测不到 | 补齐常见位置 + 扫盘 + 结果缓存 |

---

## 1. 架构原理

### ZCode 的 system prompt 拼装流程（原版）

```
zcode.cjs 内部 ContextBuilder.build()
  ├── JMe()  = "CLI Prefix"            ← "You are ZCode, ..."
  ├── dGs()  = "Custom System Prompt"  ← 默认空
  └── !customSystemPrompt 时才注入：
        Heo()  "System Context" / wmn() "ZCode Desktop Context"
        Veo()  guidance / nmn() "Environment Info"
        bmn()  "Memory" / rmn() "System Context"
  另外无条件注入：
        KMe()  "Skills"              (meta_user)
        Yut()  "Request User Context" (meta_user)
        nNe()  "Current Date"         (meta_user)
```

关键：**只要 `customSystemPrompt` 有值，ZCode 自己就会跳过 System Context /
Environment Info / Memory / Desktop Context 这一整块**。所以补丁 2 是主要手段。

### 部署后的流程

```
build()
  ├── JMe()  → ""                     ← 补丁 1：身份声明置空
  ├── dGs()  → readFileSync(人格.txt)  ← 补丁 2：唯一 system 内容
  ├── 上面那一整块 !customSystemPrompt 分支 → 自动跳过
  ├── KMe()  → null                   ← 补丁 5
  ├── Yut()  → null                   ← 补丁 6
  └── nNe()  → null                   ← 补丁 4
         ↓
  SYSTEM 层 = 人格.txt 全文，无其他内容
```

---

## 2. 六个补丁明细（v2 锚点）

| # | 稳定锚点（语义字面量） | 修改方式 | 作用 |
|---|------------------------|----------|------|
| 1 | `"You are ZCode, an interactive coding agent"` | `VAR="..."` → `VAR=""` | 删除产品身份声明 |
| 2 | `this.config.customSystemPrompt?.trim()` | 赋值改为 `readFileSync(人格.txt)` | system prompt 换外部文件 |
| 3 | `name:"Agent Identity",source:"identity"` | 所在函数改读外部文件（保留原逻辑兜底） | Agent Identity 同步替换 |
| 4 | `name:"Current Date",source:"current_date"` | 所在函数开头插 `return null;` | 移除日期提醒 |
| 5 | `name:"Skills",source:"skills"` | 同上 | 移除 Skills 列表 |
| 6 | `name:"Request User Context"` | 同上 | 移除 User Context |

### 新旧标识符对照

| 补丁 | 旧版标识符 | 新版标识符 | 稳定锚点（v2 使用） |
|------|-----------|-----------|---------------------|
| 1 CLI Prefix | `Djo` | `FVs` | `"You are ZCode, an interactive coding agent"` |
| 2 Custom System Prompt | `n=...;o=!!n` | `o=...;s=!!o` | `this.config.customSystemPrompt?.trim()` |
| 3 Agent Identity | `Eut` | `Qfn` | `name:"Agent Identity",source:"identity"` |
| 4 日期提醒 | `fre` | `nNe` | `name:"Current Date",source:"current_date"` |
| 5 Skills | `sre` | `KMe` | `name:"Skills",source:"skills"` |
| 6 User Context | `F1e` | `Yut` | `name:"Request User Context",source:"request_user_context"` |

**注意**：新版里 `Qfn()`（Agent Identity）在 `customSystemPrompt` 有值时**根本不会被调用**，
补丁 3 属于双保险；补丁 1/4/5/6 仍然必需。

**路径说明**：补丁 2/3 里写死了人格文件绝对路径。移动文件夹后必须重跑 `部署.bat`，
否则读文件失败会静默回退原版。

---

## 3. ZCode 新版全部注入片段

`deploy.ps1 diag` 会实时列出这些（当前实测 10 个）：

| # | name | source | target | 产物函数 |
|---|------|--------|--------|----------|
| 1 | CLI Prefix | cli_prefix | system | JMe |
| 2 | Agent Identity | identity | system | Qfn |
| 3 | Workflow Actor Identity | workflow_actor_identity | system | emn |
| 4 | Environment Info | env_info | system | nmn |
| 5 | System Context | system_context | system | rmn |
| 6 | Skills | skills | meta_user | KMe |
| 7 | Request User Context | request_user_context | meta_user | Yut |
| 8 | Current Date | current_date | meta_user | nNe |
| 9 | Memory | memory | system | bmn |
| 10 | Custom System Prompt | custom_system_prompt | system | （build() 内联） |

其中 3/4/5/9 属于 `!customSystemPrompt` 分支，补丁 2 生效后自动不注入。

---

## 4. 文件清单

```
ZCode-Deploy-main\
├── 部署.bat              一键部署（调 deploy.ps1 install）
├── 恢复.bat              恢复原版（调 deploy.ps1 restore）
├── 查看状态.bat           查状态（调 deploy.ps1 status）
├── 验证.bat              诊断 + 功能验证
├── deploy.ps1            主脚本
├── verify-patch.cjs      功能验证器（真正执行补丁后的函数）
├── 人格.txt              人格文件（唯一编辑入口）
├── backups\              原版归档（保留最近 6 份）
├── zcode-dir.txt         上次检测到的 ZCode 目录缓存
└── 修改记录.md           本文档

D:\app\zcode\
├── resources\glm\zcode.cjs        ← 被修改的目标
└── resources\glm\zcode.cjs.bak    ← 自动备份的原版
```

### deploy.ps1 的路径自动检测顺序

1. `ZCODE_DIR` 环境变量
2. 运行中的 ZCode 进程路径
3. `zcode-dir.txt` 缓存
4. 常见位置（`D:\app\zcode`、`C:\app\zcode`、`F:\zcode`、`%LOCALAPPDATA%\Programs\zcode`、`C:\Program Files\zcode` …）
5. 各盘符根目录 `<盘>:\zcode` / `<盘>:\app\zcode`
6. 扫盘（深度 3，兜底）

---

## 5. 日常操作

### 换人格（最常用）

```
1. 编辑 人格.txt
2. 保存
3. 开新对话 → 生效
（不需要重启 ZCode，不需要重跑部署）
```

### 部署 / 恢复 / 状态 / 诊断

```powershell
powershell -ExecutionPolicy Bypass -File deploy.ps1 install
powershell -ExecutionPolicy Bypass -File deploy.ps1 restore
powershell -ExecutionPolicy Bypass -File deploy.ps1 status
powershell -ExecutionPolicy Bypass -File deploy.ps1 diag
```

或双击对应的 .bat。

### restore 的行为

- 还原 `zcode.cjs` ← `.bak`
- **保留** `人格.txt`
- 还原前先归档当前已破解版本到 `backups\`
- 重启 ZCode

---

## 6. 验证方法

### 方法一：`验证.bat`（推荐，秒级）

1. `deploy.ps1 diag` —— 列出全部注入片段与产物函数
2. `verify-patch.cjs` —— 从 cjs 里抽出补丁后的函数**真正执行**：
   - 注入的读取表达式确实读得到 `人格.txt`
   - `nNe` / `KMe` / `Yut` 实际返回 `null`
   - `JMe` 实际返回空串
   - `Qfn` 的 content 就是 `人格.txt` 全文

### 方法二：看模型请求日志

```
%USERPROFILE%\.zcode\cli\rollout\model-io-*.jsonl
```

检查点：system 层应为 `人格.txt` 全文，不含 `"You are ZCode"`、不含安全策略、不含日期提醒。

---

## 7. 已知问题与排查

| 问题 | 排查 / 处理 |
|------|------------|
| ZCode 更新后失效 | 重跑 `部署.bat`。v2 会自动识别新版原版、刷新备份、重新打补丁；若注入点结构真的变了，会**明确报错并打印 diag**，不再静默无效果 |
| 补丁报「未命中」并退出 1 | 说明 ZCode 又换了结构。把 `diag` 输出发给维护者，按第 2 节的新锚点表更新。应急可用 `-AllowPartial` 强制部署 |
| 文件夹移动后失效 | 路径写死在 cjs 里，重跑 `部署.bat` |
| `deploy.ps1` 丢 BOM 后报语法错 | 中文 UTF-8 无 BOM 时 PS 5.1 按 GBK 解析，出现 `Unexpected token` / `The string is missing the terminator` 等大量假报错。修复：用编辑器另存为 **UTF-8 with BOM**（`EF BB BF` 开头） |
| 模型层抵抗 | 产品层已干净，但模型自身 RLHF 可能不完全采纳人格（无法在补丁层解决） |
| `workflowActor` 与 `customSystemPrompt` 互斥 | 新版 build() 里有 `if(workflowActor!==void 0 && customSystemPrompt) throw`。补丁 2 强制让 `customSystemPrompt` 有值，如果配置里同时启用了 workflow actor 会抛错。日常使用不受影响 |

---

## 8. 与 WorkBuddy 部署的区别

| | ZCode | WorkBuddy |
|---|---|---|
| 文件夹 | `ZCode-Deploy-main` | `WorkBuddy-Deploy` |
| 目标 | `zcode.cjs` 的 `ContextBuilder.build()` | `codebuddy.js` 的 `ns()` |
| 入口 | `customSystemPrompt` | 直接函数开头注入 |
| 补丁数 | 6 个 | 模板清空 + 缓存策略删除 + 隐藏指令关闭 |
| 互不干扰 | 是 | 是 |
